# 1.1.7.1

## 功能更新
- 优化首页课程卡片在单节课场景下的紧凑布局，避免课程名超出卡片上下边界。
- 调整 `2×2` 桌面小组件课程列表，可显示至少 3 节课，并在超出时提示“还有 X 节”。
- `2×2` 主卡和 `2×2` 课程列表小组件都补上了教室显示。
- 调整 `2×2` 课程列表当前课程高亮样式，改成更利落的小圆角卡片。
- 优化小米超级岛左侧纯文字图片的缩放逻辑，关闭图标后最小字号调节更明显。
