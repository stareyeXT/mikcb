# v1.1.10.13

- 修复超级岛后台提醒的周次判断错误：原生调度现在会识别 `customWeeks`，并按和 Flutter 一致的规则处理单双周，避免单周误提醒双周课、或没课时仍然上岛。
- 修复原生后台当前周计算与课表页不一致的问题：现在按周一对齐并限制在学期总周数内，减少开学日期不是周一时的错周提醒。
- 更新检测优化探测下载实现，服务器忽略 Range 请求时不再把整包 APK 预读进内存。
- 创作者支持页拉取赞助名单时会优先尝试镜像地址，镜像失败后再回退 GitHub 源。
